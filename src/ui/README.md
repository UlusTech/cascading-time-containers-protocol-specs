We only look at one single page. The contents of it change.

This is like a dashboard.

All the srounding UI is the same, also the container we view is defined on the url. Even the url on the top changes, they are not diffrent pages. We have a single page.